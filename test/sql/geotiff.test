# name: test/sql/geotiff.test
# group: [sql]

# file: test/sql/geotiff.test
# group [geotiff]

statement ok
LOAD geotiff;

# use a tiny test file placed at test/data/cea.tif (or fetched in the workflow)
query I
SELECT COUNT(*) FROM read_geotiff('test/data/cea.tif', band := 1);
----
264710