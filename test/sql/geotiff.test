# name: test/sql/geotiff.test
# group: [sql]

# group [geotiff]

statement ok
INSTALL geotiff FROM local_build_release;

statement ok
LOAD geotiff;


query I
SELECT COUNT(*) FROM read_geotiff('test/data/cea.tif', band := 1, target_mb := 32);
----
264710
